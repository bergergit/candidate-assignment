import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fabioberger.pingertakehome',
  appName: 'candidate-assignment',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
